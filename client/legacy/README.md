# Retired passenger flows

Source moved out of the active client build, including pre-existing local edits.
Nothing here is imported by the application. Retired URLs redirect or show a
retirement explanation without retrieving archived passenger records.

Do not reconnect old create/start/join/exit or card-registration flows. The
active passenger entry points are `/travel`, `/routes`, `/stops`, and `/saved`.
